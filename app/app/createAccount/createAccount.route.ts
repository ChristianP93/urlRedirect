import { Routes } from '@angular/router';

import { NgCreateAccountComponent } from './createAccount.component';

export const ngCreateAccountRoute: Routes = [
    { path: 'create/account', component: NgCreateAccountComponent }
];
